import Image from "next/image";
import Link from "next/link";

export default function Home() {
  return (
    <main className="min-h-screen bg-white text-gray-900">
      {/* HERO SECTION */}
      <section className="relative h-screen bg-cover bg-center flex items-center justify-center" style={{ backgroundImage: "url('/hero-boat.jpg')" }}>
        <div className="bg-black bg-opacity-50 p-6 rounded-xl text-center">
          <h1 className="text-4xl md:text-6xl font-bold text-white mb-4">Location de bateaux à Cannes</h1>
          <p className="text-white text-lg md:text-xl mb-6">De 7 à 24 mètres – Avec ou sans capitaine</p>
          <Link href="#contact" className="bg-white text-blue-800 font-semibold px-6 py-3 rounded-xl">Demander un devis</Link>
        </div>
      </section>

      {/* SERVICES SECTION */}
      <section className="px-6 py-16 bg-blue-50">
        <div className="max-w-6xl mx-auto text-center">
          <h2 className="text-3xl font-bold mb-8">Nos services</h2>
          <div className="grid md:grid-cols-4 gap-8">
            {[
              { title: "Capitaine professionnel", icon: "👨‍✈️" },
              { title: "Apéritifs & boissons à bord", icon: "🍾" },
              { title: "Jet ski & seabob", icon: "🛥️" },
              { title: "Personnalisation événements", icon: "🎉" },
            ].map(({ title, icon }, i) => (
              <div key={i} className="bg-white shadow-md p-6 rounded-xl">
                <div className="text-4xl mb-4">{icon}</div>
                <h3 className="text-xl font-semibold">{title}</h3>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* BOATS SECTION */}
      <section className="px-6 py-16 bg-white" id="boats">
        <div className="max-w-6xl mx-auto">
          <h2 className="text-3xl font-bold mb-8 text-center">Nos bateaux</h2>
          <div className="grid md:grid-cols-3 gap-8">
            {[
              { name: "Saxdor 320 GTO", size: "10 m", people: 8, image: "/saxdor.jpg" },
              { name: "Princess 64", size: "20 m", people: 12, image: "/princess64.jpg" },
              { name: "Rizzardi", size: "24 m", people: 12, image: "/rizzardi.jpg" },
            ].map((boat, i) => (
              <div key={i} className="shadow-md rounded-xl overflow-hidden">
                <Image src={boat.image} alt={boat.name} width={500} height={300} className="w-full h-64 object-cover" />
                <div className="p-4">
                  <h3 className="text-xl font-bold mb-2">{boat.name}</h3>
                  <p className="text-gray-600">Longueur : {boat.size} – Capacité : {boat.people} personnes</p>
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* CONTACT SECTION */}
      <section className="px-6 py-16 bg-blue-100" id="contact">
        <div className="max-w-3xl mx-auto text-center">
          <h2 className="text-3xl font-bold mb-6">Contactez-nous</h2>
          <p className="mb-6">Réponse rapide par WhatsApp, mail ou téléphone</p>
          <form className="grid gap-4">
            <input type="text" placeholder="Nom" className="p-3 rounded-lg border" />
            <input type="email" placeholder="E-mail" className="p-3 rounded-lg border" />
            <textarea placeholder="Message ou demande de devis..." className="p-3 rounded-lg border min-h-[120px]" />
            <button className="bg-blue-800 text-white font-semibold py-3 rounded-xl">Envoyer</button>
          </form>
        </div>
      </section>

      {/* FOOTER */}
      <footer className="px-6 py-6 text-center bg-white border-t">
        <p className="text-sm text-gray-500">© {new Date().getFullYear()} Location Bateaux Cannes. Tous droits réservés.</p>
      </footer>
    </main>
  );
}
