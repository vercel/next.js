npx create-next-app chefmaster
cd chefmaster
npm run dev
chefmaster/
├── app/
│   ├── página/
│   │   ├── index.tsx         ← Página principal
│   │   ├── cursos.tsx
│   │   ├── recetas.tsx
│   │   ├── tecnicas.tsx
│   │   ├── ingredientes.tsx
│
├── componentes/
│   ├── CardReceta.tsx
│   ├── FiltroNiveles.tsx
│   ├── VideoPlayer.tsx
│
├── datos/
│   ├── recetas.json
│   ├── modulos.json
│   ├── tecnicas.json
│   ├── ingredientes.json
// app/página/recetas.tsx
import recetas from '@/datos/recetas.json';

export default function RecetasPage() {
  return (
    <div>
      <h1>Recetas</h1>
      {recetas.map((receta) => (
        <div key={receta.id}>
          <h2>{receta.nombre}</h2>
          <p>{receta.descripcion}</p>
        </div>
      ))}
    </div>
  );
}
[
  {
    "id": 1,
    "nombre": "Sopa de Tomate",
    "descripcion": "Una sopa básica y deliciosa con tomates frescos."
  }
]
