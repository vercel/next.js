load("@build_bazel_rules_nodejs//:index.bzl", "pkg_npm")

PACKAGE_NAME = "next-bundle-analyzer"

pkg_npm(
    name = PACKAGE_NAME,
    srcs = ["package.json", ":index.js", ":readme.md"]
)

alias(
    name = "publish",
    actual = "%s.publish" % PACKAGE_NAME,
)

alias(
    name = "pack",
    actual = "%s.pack" % PACKAGE_NAME,
)