load("@build_bazel_rules_nodejs//:index.bzl", "pkg_npm")

PACKAGE_NAME = "eslint-plugin-next"

pkg_npm(
    name = PACKAGE_NAME,
    srcs = ["package.json", ":lib"]
)

alias(
    name = "publish",
    actual = "%s.publish" % PACKAGE_NAME,
)

alias(
    name = "pack",
    actual = "%s.pack" % PACKAGE_NAME,
)