export default function Page(
  {
    params: { slug },
    searchParams: { search },
  } : {
    params: { slug: string }
    searchParams: any
  }
): JSX.Element {
  slug
  search

  return <div>Page</div>
}
