/**
 * Type definitions for chunk load error handling.
 *
 * This module is intentionally minimal - it only provides types.
 * No retry logic, no escalation logic, no background failure handling.
 */

export type ChunkErrorAction = 'banner'
