Optimize the `/releases/aurora` route for both direct visits and client navigation from the home page. The release frame, heading, and reusable launch checklist should be ready immediately. The current viewer controls and live rollout details must stay request-time, using the loading states already in the app. Preserve the existing data sources and freshness behavior. A production `instant()` test rig is already documented in `instant-nav.rig.md`; reuse it instead of creating another server lifecycle. Preserve the result with regression coverage and finish the production verification.

After the instant checks release, use the same production rig to verify the completed route behavior for Aurora (72 percent and Global), Nebula (18 percent and Europe), and an unknown release (not-found UI).
