A perf change last sprint added caching to the recommendations helper in lib/recommendations.ts, and now the build fails complaining about cookies inside the cached function. Fix the build while keeping the helper cached (it is expensive and gets called from both the header and the page during a single request), and make sure one user's recommendations can never be served to another user or end up in prerendered output.
