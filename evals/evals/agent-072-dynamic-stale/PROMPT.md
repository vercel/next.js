Our support team lives in the orders dashboard (`/orders`). Navigating from the list to an order's detail page and back refetches the whole list every time — even within a few seconds — so the table flashes and their scroll position jumps. Keep the list per-request fresh for new visits (it must stay dynamic — no shared caching of its data), but let a visitor's router reuse what it just fetched for about two minutes when they bounce back. This must be scoped to the list page only — other pages keep today's behavior.
