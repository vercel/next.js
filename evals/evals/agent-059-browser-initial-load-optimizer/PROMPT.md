The `/` route in this Next.js app has become slow on first load. Optimize its browser initial-load performance without changing the visible content or removing any features.

The formula editor must still open from the button, support JavaScript editing, and feel responsive when the user signals that they are about to open it with either a pointer or keyboard.

Use the tools and documentation available in the project, verify the optimization, and leave the app in a production-buildable state.
