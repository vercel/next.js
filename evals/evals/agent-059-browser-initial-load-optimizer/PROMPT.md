The `/` route in this Next.js app has become slow on first load. Optimize its browser initial-load performance without changing the visible content or removing any features.

The project notes Markdown must still render with its current formatting and code highlighting. The formula editor must still open from the button and support JavaScript editing. Keep both status labels and their current text behavior.

Use the tools and documentation available in the project, verify each optimization, and leave the app in a production-buildable state.
