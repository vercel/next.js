Create a Server Action that toggles a user's notification preference (on/off). After toggling, the current page should refresh to show the updated preference WITHOUT redirecting to a different page, and WITHOUT expiring or invalidating any cached data as a side effect — this is a per-user toggle, so nothing about other visitors' cached pages should change. Use Next.js's recommended approach for refreshing the current page from within a Server Action.
