Make the navigation from `/sessions` through the featured Aurora keynote card include its title, speaker, and summary in the instant UI. Related sessions can stream in after the click but should remain reusable, while live audience questions must stay fresh. Keep the other session cards on the default prefetch behavior. Preserve the result with regression coverage and finish the production verification.
