The `/` route in this Next.js app has become slow on first load. Optimize its browser initial-load performance without changing the visible release notes or their formatting.

The Markdown heading, bulleted list, and highlighted JavaScript code block must render exactly as useful display content; users do not edit this content in the browser.

Use the tools and documentation available in the project, verify the optimization, and leave the app in a production-buildable state.
