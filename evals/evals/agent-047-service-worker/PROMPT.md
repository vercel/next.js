Offline support is broken: the registrar points at /sw.js which 404s, while the actual worker code lives in lib/offline-worker.ts and never ships. Make the TypeScript worker get bundled and served by the framework itself — no hand-copied files in public/, no extra build step, no third-party PWA packages — and keep it controlling the whole site so it never gets pinned to an old version by CDN caching.
