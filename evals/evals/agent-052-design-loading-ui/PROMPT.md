This team directory's loading UI feels broken. First load stacks several loading signals, briefly claims there are no members, and shifts when the real rows arrive. Search blanks useful results and can show an older response, while Load more replaces the list with a spinner. Member profiles reveal three related sections as a sequence of unrelated spinners. Make the experience stable and coherent without removing the stats, directory, search, pagination, profiles, missing-member behavior, or artificial latency.
