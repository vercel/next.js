# Release v3.0

CHANGELOG_BODY_MARKER_v3-0

## Highlights

- Offline mode with local caching of opened documents.
- Pluggable renderers for embedded diagrams.
- New theming API for downstream deployments.

## Notes

Major release. The theming API replaces the old CSS override mechanism, which is now deprecated.
