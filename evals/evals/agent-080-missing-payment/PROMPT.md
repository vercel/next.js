Our billing dashboard has a "record payment" form on the invoice page; saving takes about a second and a half because it posts to the payment provider. Support keeps getting reports that when a user hits Save and then clicks straight over to the Payments list, the payment they just recorded is missing — it only appears after a manual reload. We can't reproduce it: every time we try it ourselves, the row is there. Users insist the list finished loading after the save spinner completed.

The payments list must always include every payment whose save the user initiated before landing on it. Don't slow anything down: saving must keep taking exactly the time the provider takes (lib/payments-core.ts simulates the provider and is owned by the integrations team — don't modify it), the form must stay usable (no blocking the whole page while saving), and navigation must stay client-side (real Links). Keep the existing data-testid attributes.
