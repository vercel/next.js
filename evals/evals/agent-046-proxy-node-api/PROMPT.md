Before any request under /admin reaches a page, verify its "x-signature" header: it must equal a hex SHA-256 HMAC of the request pathname keyed by the ADMIN_SECRET environment variable, compared with a timing-safe comparison, and failures must be redirected to /denied. A teammate insists request interceptors in Next.js cannot use Node's crypto for this — prove them wrong or find the supported way.
