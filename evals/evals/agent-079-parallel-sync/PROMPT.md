The Sync button on our ops dashboard kicks off two completely independent syncs — inventory and pricing — but the button takes about two seconds because they run one after the other. Each sync takes about a second on the vendor's side, and the two touch entirely different systems, so there's no reason the button should take longer than roughly one second end-to-end.

Make the button finish in about half the time it takes today, keeping both result numbers displayed exactly as they are now. Both syncs must remain server-side operations (they use server-only credentials), and both have to keep going through the integrations team's client in lib/sync-core.ts. That file simulates the two vendors' real latency and is owned by the integrations team — don't modify it. Also keep the existing data-testid attributes on the panel; our smoke tests select on them.
