The `/` route in this Next.js app has become unnecessarily large in the browser. Optimize its browser bundle without changing either deployment status label or how those labels are produced.

Both the current status and the status from the local legacy widget must keep their existing text transformations.

Use the tools and documentation available in the project, verify the optimization, and leave the app in a production-buildable state with an accurate lockfile.
