# Bench fixture: local, committed SQLite database. No secrets.
# Path is resolved relative to the app dir (the harness runs next with cwd here).
DATABASE_URL="file:./prisma/dev.db"
NEXT_TELEMETRY_DISABLED=1
