{
  "private": true,
  "description": "Notify issues/PRs that were fixed in the latest release",
  "scripts": {
    "build": "ncc build src/index.mjs -m -o dist --license licenses.txt"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
