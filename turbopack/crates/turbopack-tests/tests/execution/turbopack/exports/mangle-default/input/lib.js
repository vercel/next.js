// `default` is mangled like any other export key: every read of it in an eligible module is
// generated by us. Modules whose names can be observed (CommonJS interop, an escaping namespace,
// a dynamic import) keep `default` along with everything else — see the other `mangle-*` fixtures.

export default function theDefaultExport() {
  return 'default-value'
}

export const someLongExportName = 'named-value'

export const exportsInfo = {
  default: __webpack_exports_info__.default,
  someLongExportName: __webpack_exports_info__.someLongExportName,
}
