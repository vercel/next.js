// A namespace object of this module escapes into user code (see `provider.js`), so its export
// names are observable and must not be shortened.

export const ENUM_A = 'a-value'
export const ENUM_B = 'b-value'
export const ENUM_C = 'c-value'
export const NUM = 42
export default 'default-value'
