# Chunking

xs

Deciding which modules go into which bundles. In a wiki, the analogous global
computation is the backlinks index: it inverts the link graph, so it depends
on every page. See [[Turbo Tasks]] and [[Incremental Computation]].
