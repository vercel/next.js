# Turbo Tasks

An incremental computation engine. Functions are memoized, reads are tracked,
and invalidation propagates bottom-up. See [[Incremental Computation]] for the
general idea.

## Primitives

Functions, values, traits, and collectibles.
