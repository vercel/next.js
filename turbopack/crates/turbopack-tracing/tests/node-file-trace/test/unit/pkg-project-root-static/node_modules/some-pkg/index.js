const fs = require('fs')
const path = require('path')

let configPath = './app-config.js'
if (process.env.CONFIG_PATH) {
  configPath = process.env.CONFIG_PATH
}

fs.existsSync(path.resolve(configPath))
