[package]
name = "turbo-tasks-test-utils"
version = "0.1.0"
edition = "2021"

[lib]
bench = false

[dependencies]
lazy_static = "1.4.0"
turbo-tasks = { path = "../turbo-tasks" }
turbo-tasks-memory = { path = "../turbo-tasks-memory" }
