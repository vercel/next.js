// Statically pre-rendered Pages Router page (no data-fetching) — produces a
// `.html` server entry, exercises the `pages-static` route type.
export default function StaticPage() {
  return <p>pages-static</p>
}
