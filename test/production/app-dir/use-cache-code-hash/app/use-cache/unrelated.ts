// This module is part of the build (imported by the page) but is NOT in the
// import subtree of the cached function, so editing it must not change the
// action's codeHash.
export function unrelated() {
  return 'unrelated-v1'
}
