// import { nextTestSetup } from 'e2e-utils'

// TODO(segment-viewer): enable test once stabilized
describe.skip('segment-viewer', () => {
  // const { next } = nextTestSetup({
  //   files: __dirname,
  // })
})
