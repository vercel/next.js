// BLOCKED: This test creates an external HTTP server (http.createServer) and rewrites
// to it via next.config.js. The external HTTP server lifecycle cannot be modeled with
// nextTestSetup(). Original: test/integration/custom-routes-i18n/test/index.test.ts
it.todo('custom-routes-i18n')
