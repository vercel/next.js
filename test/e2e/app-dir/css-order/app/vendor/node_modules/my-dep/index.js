import { jsx as _jsx } from 'react/jsx-runtime'
import styles from './index.module.css'

export function MyLink({ children, className, ...props }) {
  return /*#__PURE__*/ _jsx('a', {
    ...props,
    className: `${styles.button_my_dep} ${className ?? ''}`,
    children: children,
  })
}
