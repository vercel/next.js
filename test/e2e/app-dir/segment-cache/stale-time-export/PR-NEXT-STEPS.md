# Stale Time Export PR Next Steps

- [ ] Add a test suite that verifies behavior when `cacheComponents` is enabled in a fixture app together with `unstable_staleTime`.
- [ ] Add tests to verify `unstable_staleTime` and `unstable_instant` cannot be used together on the same page.
