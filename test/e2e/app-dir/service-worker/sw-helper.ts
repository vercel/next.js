// Dynamically imported by the service-worker entry. In single-chunk mode this
// must end up inlined in the single output bundle (no separate chunk fetched).
export function greeting() {
  return 'hello from sw helper'
}
