// This is the default (non-web) version of the module.
// If resolveExtensions ordering is respected, this file should NOT be used
// when .web.js is listed before .js in the config.
export default function greeting() {
  return 'hello from default'
}
