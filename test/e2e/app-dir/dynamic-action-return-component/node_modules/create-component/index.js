import React from 'react'
import { Client } from './client'

export function createComponent({ action }) {
  return async function NewComponent() {
    return (
      React.createElement('div', null, React.createElement(Client, { action }))
    )
  }
}

