export default function NotFound() {
  return <div id="static-not-found">Static metadata not found</div>
}

export const metadata = {
  title: 'Static Not Found Title',
  description: 'Static not found description value',
}
