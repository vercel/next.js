import { feature } from './feature.js'

function getDefaultIntegrations() {
  const integrations = []

  if (typeof __MY_FLAG__ === 'undefined' || __MY_FLAG__) {
    integrations.push(feature())
  }

  return integrations
}

export function init() {
  return { defaultIntegrations: getDefaultIntegrations() }
}
