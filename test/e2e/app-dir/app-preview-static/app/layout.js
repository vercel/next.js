export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <head>
        <title>static with previewData</title>
      </head>

      <body>{children}</body>
    </html>
  )
}
