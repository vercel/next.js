export const unstable_instant = { prefetch: 'static' }

export default function Page() {
  return (
    <main>
      This is a static page with an icon component that does not use params.
    </main>
  )
}
